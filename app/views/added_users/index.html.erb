<div class="container">
  <div class="row">
    <div class="col">
      <h4 class="mt-5 mb-4">All Users</h4>
    </div>
    <div class="col-auto">
      <%= link_to "Add User", new_added_user_path, class: "btn btn-primary float-end me-2" %>
    </div>
  </div>

  <% if @added_users.present? %>
    <table class="table table-striped">
      <thead class="thead-dark">
        <tr>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Email</th>
          <th>Phone</th>
          <th>Date of Birth</th>
          <th>Gender</th>
          <th>Address</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @added_users.each do |user| %>
          <tr>
            <td><%= user.first_name %></td>
            <td><%= user.last_name %></td>
            <td><%= user.email %></td>
            <td><%= user.phone %></td>
            <td><%= user.date_of_birth %></td>
            <td><%= user.gender %></td>
            <td><%= user.address %></td>
            <td>
              <%= link_to "Edit", edit_added_user_path(user), class: "btn btn-primary btn-sm me-2" %>
              <%= button_to "Delete", added_user_path(user), method: :delete,
                         data: { confirm: "Are you sure you want to delete this user?" },
                         class: "btn btn-danger btn-sm" %>           
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <% if @total_pages > 1 %>
<%= render 'shared/paginate', path: added_users_path, page_number: @page_number, total_pages: @total_pages %>

    <% end %>

  <% else %>
    <div class="container">
      <p>
        There are no users to show. But you can create by using Add User Button.
      </p>
    </div>
  <% end %>
</div>
